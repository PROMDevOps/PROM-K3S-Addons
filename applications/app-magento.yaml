apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: magento
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          custom: "true"
  template:
    metadata:
      name: 'magento-{{name}}'
    spec:
      project: "default"
      source:
        repoURL: https://github.com/PROMDevOps/Cybertracx-Deployment.git
        targetRevision: "{{name}}" #Based on environment
        path: 'cybertracx-magento'
        helm:
          ignoreMissingValueFiles: true
          valueFiles:
            - values.yaml
            - values-{{name}}.yaml
      destination:
        name: in-cluster
        namespace: magento-{{name}}
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true
